# Generated at runtime by run-reth.sh.
jwt.hex

# fixtures/ holds the committed golden Engine API exchanges (captured from a
# real reth v2.2.0 green run) — tracked on purpose, not ignored.

# Generated by build.rs from genesis.template.json + contracts/*.sol — the
# predeploy bytecode is compiled from source at build time, never committed.
/genesis.json
