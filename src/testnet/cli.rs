//! Argument parser + dispatcher for the `testnet` binary.
//!
//! TODO(#189): not yet implemented. The binary entry point in
//! `src/bin/testnet.rs` will live here; until then the supporting
//! library modules ([`super::topology`], [`super::workdir`],
//! [`super::lifecycle`], [`super::admin`], [`super::wait`],
//! [`super::safety`], [`super::telemetry`], [`super::scenario`])
//! are usable directly from integration tests and follow-up work.
