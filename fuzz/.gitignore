target
artifacts
Cargo.lock

# libFuzzer writes discovered inputs into the corpus dir under SHA1-hex
# names. Only the curated `.bin` seeds produced by `seed_corpus` are
# committed; runtime discoveries are local and would just bloat the
# repo.
corpus/**/*
!corpus/
!corpus/*/
!corpus/*/*.bin
